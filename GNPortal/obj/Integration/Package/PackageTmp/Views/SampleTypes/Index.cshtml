@model IEnumerable<GenomeNext.Data.EntityModel.GNSampleType>

@{
    ViewBag.Title = "Sample Types";
}

<h2>
    <span class="glyphicon glyphicon-tint gn-green"></span>
    @ViewBag.Title
</h2>

@Html.BootstrapRowSpacer()

<div class="table-responsive">
    <table class="table table-hover table-bordered table-gn">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TypeValue)
            </th>
            <th class="col-md-1"></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TypeValue)
                </td>
                <td>
                    @Html.BootstrapActionButton("View", "Details", "SampleTypes", new RouteValueDictionary(new { id = item.Id }), "success", "th-large", "sm")
                </td>
            </tr>
        }

    </table>
</div>