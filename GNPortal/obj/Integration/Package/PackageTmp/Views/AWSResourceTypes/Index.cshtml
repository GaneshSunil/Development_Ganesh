@model IEnumerable<GenomeNext.Data.EntityModel.AWSResourceType>

@{
    ViewBag.Title = "AWS Resource Types";
}

<h2>
    <span class="glyphicon glyphicon-cloud gn-green"></span>
    @ViewBag.Title
    <span class="pull-right">
        @Html.BootstrapActionButton("Create New", "Create", "AWSResourceTypes", new RouteValueDictionary(new { organizationId = Request["organizationId"] }), "info", "plus")
    </span>
</h2>


@Html.BootstrapRowSpacer()

<div class="table-responsive">
    <table class="table table-hover table-bordered table-gn">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.BootstrapActionButton("View", "Details", "AWSResourceTypes", new RouteValueDictionary(new { id = item.Id }), "success", "th-large", "sm")
                    @Html.BootstrapActionButton("Edit", "Edit", "AWSResourceTypes", new RouteValueDictionary(new { id = item.Id }), "warning", "cog", "sm")
                    @Html.BootstrapActionButton("Delete", "Delete", "AWSResourceTypes", new RouteValueDictionary(new { id = item.Id }), "danger", "trash", "sm")
                </td>
            </tr>
        }

    </table>
</div>